import React, {FC} from "react";
import styled from 'styled-components/native'


export const FeedScreen: FC = () => {
    return (
        <FeedScreenContainer>

        </FeedScreenContainer>
    );
};

const FeedScreenContainer = styled.View`
  flex: 1;
  align-items: center;
  justify-content: center;
`;
